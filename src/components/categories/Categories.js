import { Box } from "@material-ui/core";
import CategoryList from './CategoryList'
const Categories = (props) => {
    return (
        <Box>
            <CategoryList listTitle='Income Categories'/>
        </Box>
    )
}

export default Categories;
